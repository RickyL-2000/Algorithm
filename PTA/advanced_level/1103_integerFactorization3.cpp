#include <iostream>
#include <math.h>
#include <vector>
using namespace std;
int n, k, p, index;
vector<int> factors, temp_v, pows;

void dfs() {
    if (index == k-1) {

    }
}

int main() {
    cin >> n >> k >> p;
    factors.resize(k), temp_v.resize(k);
    
    index = 0;

    // printf("%d = ", n);
    // for (size_t i = 0; i < k; i++) {
    //     printf("%d^%d", factors[i], p);
    //     if (i != k-1) printf(" + ");
    // }
    
    return 0;
}